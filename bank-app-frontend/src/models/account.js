export class Account {
    constructor(id, accountId, accountType, balance) {
        this.id = id;
        this.accountId = accountId;
        this.accountType = accountType;
        this.balance = balance;
    }
}
